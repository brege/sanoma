---
name: Update Dataset
description: Extract fresh dataset and verify
steps:
  - name: extract fresh dataset
    action: extract
    params:
      format: json
      output: data/extract/all.json
      force: true
  - name: verify dataset
    action: stats
    params:
      input: data/extract/all.json
